<header class="navbar grid-paper">
  <div class="container">
    <a href="/" class="brand">
      <span class="mark">P·H</span>
      <div class="text">
        <strong>Physics of Hobbies</strong>
        <small>el porqué de las cosas</small>
      </div>
    </a>

    <nav class="menu">
      <a href="/" class="nav-link">Inicio</a>
      <a href="/sobre-mi" class="nav-link">Sobre el proyecto</a>
      <a href="/privacidad" class="nav-link">Privacidad</a>
    </nav>
  </div>
  <div class="trace-rule"></div>
</header>

<style>
  .navbar {
    background: var(--ink);
    padding: 1.5rem 0;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    text-decoration: none;
  }
  .mark {
    font-family: var(--font-mono);
    font-weight: 600;
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    color: var(--signal);
    border: 1px solid color-mix(in srgb, var(--signal) 45%, transparent);
    border-radius: 4px;
    padding: 0.3rem 0.55rem;
  }
  .text { display: flex; flex-direction: column; line-height: 1.25; }
  .text strong {
    color: #ffffff;
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 700;
  }
  .text small {
    font-family: var(--font-mono);
    color: #8b95a0;
    font-size: 0.75rem;
    letter-spacing: 0.02em;
  }

  .menu { display: flex; gap: 2rem; }
  .nav-link {
    font-family: var(--font-mono);
    color: #cbd2d9;
    text-decoration: none;
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding-bottom: 0.2rem;
    border-bottom: 1px solid transparent;
    transition: color 0.2s, border-color 0.2s;
  }
  .nav-link:hover { color: var(--signal); border-color: var(--signal); }

  /* Por debajo de ~720px el menú en la misma fila que la marca desbordaba el
     viewport y provocaba scroll horizontal en todo el sitio. */
  @media (max-width: 720px) {
    .navbar { padding: 1.1rem 0; }
    .container {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.9rem;
      padding: 0 1.25rem;
    }
    .menu {
      gap: 1.1rem;
      flex-wrap: wrap;
      row-gap: 0.5rem;
    }
    .nav-link { font-size: 0.72rem; }
    .text strong { font-size: 1.05rem; }
  }
</style>
